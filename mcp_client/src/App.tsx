import { Box } from "@mui/material";
import Chat from "./Chat";

const App = () => {
  return (
    <Box sx={{ height: "100vh", overflow: "hidden" }}>
      <Chat />
    </Box>
  );
};

export default App;
